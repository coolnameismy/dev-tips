1:加载核心模块
var http = require("http");
var fs = require('fs');

//说明：默认。js可以省略
//可以使用相对路径./也可以用绝对路径../

2:加载自定义模块

//模块需要放在node_modules中
var myModules = require("myModulesDir");//模块所在文件夹名称

3：在package.json中加入说明
{
  "name": "myTestModules",
  "version": "0.0.1",
  "description": "liuyanwei test",
  "keywords": [
    "test",
    "database"
  ],
  "author": {
    "name": "liuyanwei",
    "email": "coolnameismy@hotmail.com"
  },
  "main": "./index.js"
   
}

